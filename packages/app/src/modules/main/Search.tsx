import React from "react";
import { View, Text } from "react-native";

interface SearchProps {}

export const Search: React.FC<SearchProps> = ({}) => {
    return (
        <View>
            <Text>this is a really nice search page</Text>
        </View>
    );
};
